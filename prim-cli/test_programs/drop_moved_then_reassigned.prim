import std.io.println

struct R { id: i32 }
impl Drop for R {
    fn drop(mut self) { println(self.id) }
}

fn sink(r: R) {
    println(9i32)
}

fn main() {
    let mut r = R { id = 1 }
    sink(take r)
    r = R { id = 2 }
    println(0i32)
}
