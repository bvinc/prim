import std.io.println

// Once a value is moved into an `own` parameter, it is gone.
struct S { n: i32 }

fn consume(s: own S) -> i32 {
    return s.n
}

fn main() {
    let a = S { n = 5 }
    let z = consume(own a)
    let again = a.n
}
