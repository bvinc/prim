// Test that error messages refer to trait and struct types by name,
// not by internal id.

struct NotAGreeter { n: i32 }

trait Greeter {
    fn greet(g: Greeter) -> i32;
}

fn use_it(g: Greeter) -> i32 {
    g.greet()
}

fn main() {
    let x = NotAGreeter { n = 1 }
    use_it(x);
}
