import std.io.println

let mut COUNTER: i64 = 100;

fn bump() {
    COUNTER = COUNTER + 1
}

fn main() {
    println(COUNTER)
    bump()
    println(COUNTER)
    bump()
    bump()
    println(COUNTER)
}
