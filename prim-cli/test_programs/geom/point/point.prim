mod point

import std.io.println

struct Point {
    value: i32,
}

trait HasValue {
    fn get(self) -> i32;
}

impl HasValue for Point {
    fn get(self) -> i32 {
        self.value
    }
}

fn new_point() -> Point {
    Point { value = 42 }
}

fn announce() {
    println(42i32);
}
