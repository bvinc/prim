mod point

import std.io.println_i64

struct Point {
    value: i32,
}

trait HasValue {
    fn get(self) -> i32;
}

impl HasValue for Point {
    fn get(self) -> i32 {
        self.value
    }
}

fn new_point() -> Point {
    Point { value = 42 }
}

fn announce() {
    println_i64(42);
}
