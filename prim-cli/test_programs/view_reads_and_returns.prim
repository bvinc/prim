import std.io.println

struct S { n: i32 }

// A `read` borrow may read the value repeatedly; the caller keeps ownership.
fn read_twice(read s: S) -> i32 {
    return s.n + s.n
}

fn main() {
    let a = S { n = 21 }
    println(read_twice(read a))
    println(a.n)
}
