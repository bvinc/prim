import std.io.println

struct R { id: i32 }
impl Drop for R {
    fn drop(mut self) { println(self.id) }
}

fn sink(r: own R) {}

fn route(c: bool, r: own R) {
    if c {
        sink(own r)
    } else {
        sink(own r)
    }
}

fn main() {
    route(true, own R { id = 5 })
    println(0i32)
}
