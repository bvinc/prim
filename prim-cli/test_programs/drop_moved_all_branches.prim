import std.io.println

struct R { id: i32 }
impl Drop for R {
    fn drop(mut self) { println(self.id) }
}

fn sink(own r: R) {}

fn route(c: bool, own r: R) {
    if c {
        sink(own r)
    } else {
        sink(own r)
    }
}

fn main() {
    route(true, own R { id = 5 })
    println(0i32)
}
