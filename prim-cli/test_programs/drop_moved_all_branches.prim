import std.io.println

struct R { id: i32 }
impl Drop for R {
    fn drop(edit self) { println(self.id) }
}

fn sink(r: R) {}

fn route(c: bool, r: R) {
    if c {
        sink(take r)
    } else {
        sink(take r)
    }
}

fn main() {
    route(true, take R { id = 5 })
    println(0i32)
}
