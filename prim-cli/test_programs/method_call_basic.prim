import std.io.println

struct Counter {
    n: i32,
}

trait Bumpable {
    fn current(self) -> i32;
    fn doubled(self) -> i32;
}

impl Bumpable for Counter {
    fn current(self) -> i32 {
        self.n
    }
    fn doubled(self) -> i32 {
        self.n + self.n
    }
}

fn main() {
    let c = Counter { n = 21 }
    println(c.current())   // 21
    println(c.doubled())   // 42
}
