import std.io.println_i32

struct Counter {
    n: i32,
}

trait Bumpable {
    fn current(self) -> i32;
    fn doubled(self) -> i32;
}

impl Bumpable for Counter {
    fn current(self) -> i32 {
        self.n
    }
    fn doubled(self) -> i32 {
        self.n + self.n
    }
}

fn main() {
    let c = Counter { n = 21 }
    println_i32(c.current())   // 21
    println_i32(c.doubled())   // 42
}
