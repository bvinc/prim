import std.io.println

// A `from` clause must name an actual parameter.
struct Point { x: i32 }

struct view Sel { p: read Point }

fn choose(x: read Point) -> Sel from z {
    return Sel { p = read x }
}

fn main() {
    println(1i32)
}
