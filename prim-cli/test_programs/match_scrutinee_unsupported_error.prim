// Matching is only supported on enums, bools, integers, tuples, and structs.
// A float scrutinee has no structural cases, so it is rejected.
fn f(v: f64) -> i32 {
    return match v {
        _ => 0i32,
    }
}

fn main() {
    f(1.0f64)
}
