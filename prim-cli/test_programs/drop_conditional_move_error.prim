import std.io.println

struct R { id: i32 }
impl Drop for R {
    fn drop(mut self) { println(self.id) }
}

fn sink(r: R) {}

fn main() {
    let r = R { id = 1 }
    if true {
        sink(take r)
    }
}
