import std.io.println

// Simplest generic struct. Both fields are T, so T is inferred from
// the literal's initializers.
struct Pair[T] {
    first: T,
    second: T,
}

fn main() {
    let p = Pair { first = 10i32, second = 20i32 }
    println(p.first)
    println(p.second)
}
