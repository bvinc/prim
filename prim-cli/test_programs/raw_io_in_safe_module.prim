// Raw byte I/O reads/writes linear memory through a pointer host-side, so it
// is a raw-pointer power and must not be callable from safe code.
import std.sys.read_raw

fn do_read(p: *mut u8) -> usize {
    return read_raw(0, p, 10usize)
}

fn main() {
}
