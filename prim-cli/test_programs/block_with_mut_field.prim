import std.io.println
import std.vec.new
import std.vec.push
import std.vec.with
import std.vec.with_mut

struct S { n: i32 }

fn main() {
    let v = new[S]()
    push(mut v, own S { n = 1 })
    with_mut(mut v, 0usize, |e| { e.n = e.n + 10 })
    with(v, 0usize, |e| { println(e.n) })
}
