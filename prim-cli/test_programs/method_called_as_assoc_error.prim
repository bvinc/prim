struct Counter { n: i32 }

impl Counter {
    fn get(self) -> i32 {
        self.n
    }
}

fn main() {
    let c = Counter { n = 5 }
    // `get` takes `self`, so it must be called on a value.
    let bad = Counter.get(c)
}
