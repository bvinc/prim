import std.io.println

struct S { n: i32 }

// An `edit` borrow mutates in place; the change is visible to the caller.
fn bump(s: edit S) { s.n = s.n + 1i32 }

fn main() {
    let a = S { n = 41 }
    bump(edit a)
    println(a.n)
}
