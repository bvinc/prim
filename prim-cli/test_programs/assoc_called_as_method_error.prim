struct Counter { n: i32 }

impl Counter {
    fn make(start: i32) -> Self {
        Counter { n = start }
    }
    fn get(self) -> i32 {
        self.n
    }
}

fn main() {
    let c = Counter.make(1)
    // `make` has no `self`, so it can't be called on a value.
    let bad = c.make(2)
}
