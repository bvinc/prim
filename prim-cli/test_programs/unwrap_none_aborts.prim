import std.io.println
import std.option.Option

// Unwrapping a None aborts via panic_value -> panic. The process must exit
// nonzero (the abort sentinel is surfaced by the runner).
fn main() {
    let o: Option[i32] = Option.None
    let v = o.unwrap()
    println(v)
}
