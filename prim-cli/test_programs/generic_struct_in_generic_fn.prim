import std.io.println

struct Wrap[T] {
    inner: T,
}

// Generic function that constructs a generic struct using its own T.
fn wrap[T](x: T) -> Wrap[T] {
    return Wrap { inner = x }
}

fn main() {
    let a = wrap(42i32)
    println(a.inner)
}
