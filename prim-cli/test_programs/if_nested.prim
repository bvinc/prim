import std.io.println

fn main() {
    let x: i64 = 10i64
    let y: i64 = 20i64
    if x > 0i64 {
        if y > 0i64 {
            println(x + y)
        } else {
            println(x)
        }
    } else {
        println(0i64)
    }
}
