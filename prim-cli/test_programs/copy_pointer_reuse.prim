mod copy_pointer_reuse
import std.io.println
import std.ptr.null

// Raw pointers are Copy too; copying one leaves the original live.
unsafe fn main() {
    let p = null[i32]()
    let q = p
    let r = p
    println(42i32)
}
