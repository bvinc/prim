import std.io.println

enum Option[T] {
    None,
    Some { value: T },
}

fn unwrap_i32(x: view Option[i32]) -> i32 {
    return match x {
        Option.Some { value } => value,
        Option.None => 0i32,
    }
}

fn unwrap_i64(x: view Option[i64]) -> i64 {
    return match x {
        Option.Some { value } => value,
        Option.None => 0i64,
    }
}

fn main() {
    let a = Option.Some { value = 12i32 }
    let b = Option.Some { value = 9000000000i64 }
    let c: Option[i32] = Option.None
    println(unwrap_i32(a))
    println(unwrap_i64(b))
    println(unwrap_i32(c))
}
