import std.io.println

struct S { n: i32 }

fn sink(s: own S) -> i32 { return s.n }

// Shadowing introduces a fresh binding; the new one is live even though the
// old one was moved.
fn main() {
    let a = S { n = 1 }
    let x = sink(own a)
    let a = S { n = 42 }
    println(a.n)
}
