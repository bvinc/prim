import std.io.println

// A struct with a borrow field must acknowledge its view-kind with `view`.
// Omitting it is a definition-site error ("nothing silently assigned").
struct Point { x: i32 }

struct Window { p: read Point }

fn main() {
    println(1i32)
}
