import std.io.println

enum Color {
    Red,
    Green,
    Blue,
}

fn score(c: Color) -> i32 {
    return match c {
        Color.Red => 1i32,
        Color.Green => 2i32,
        Color.Blue => 3i32,
    }
}

fn main() {
    println(score(Color.Green))
}
