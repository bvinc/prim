import std.io.println_i32

enum Color {
    Red,
    Green,
    Blue,
}

fn score(c: Color) -> i32 {
    match c {
        Color.Red => 1i32,
        Color.Green => 2i32,
        Color.Blue => 3i32,
    }
}

fn main() {
    println_i32(score(Color.Green))
}
