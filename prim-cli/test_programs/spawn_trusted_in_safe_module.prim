import std.rt.spawn

// `spawn(f)` indirectly runs `f` in a new task, so it must respect the same
// unsafe boundary as a direct call: safe code may not spawn an
// `unsafe fn` (doing so would run raw-core code safe code may not call).
unsafe fn worker() {
}

fn main() {
    spawn(worker)
}