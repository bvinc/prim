struct S { n: i32 }

fn sink(s: S) -> i32 { return s.n }

fn main() {
    let a = S { n = 1 }
    let mut i = 0i32
    loop {
        if i >= 3i32 { break }
        let x = sink(own a)
        i = i + 1i32
    }
}
