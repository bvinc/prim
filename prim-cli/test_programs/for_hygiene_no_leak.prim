import std.io.println

// Hygiene: the loop variable is scoped to the loop and must not leak into or
// overwrite an enclosing binding of the same name.
fn main() {
    let mut i = 100i32
    for i in 0i32..3i32 {
    }
    println(i)   // 100 -- the outer `i` is untouched
}
