import std.io.println
import std.rt.spawn
import std.rt.yield

// A spawned task that cooperatively yields once.
fn worker() {
    println(100)
    yield()
    println(101)
}

fn main() {
    spawn(worker)
    println(1)
    yield()
    println(2)
}
