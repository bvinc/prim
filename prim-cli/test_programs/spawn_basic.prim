import std.io.println
import std.rt.spawn
import std.rt.yield

// A spawned task that cooperatively yields once.
fn worker() {
    println(100i32)
    yield()
    println(101i32)
}

fn main() {
    spawn(worker)
    println(1i32)
    yield()
    println(2i32)
}
