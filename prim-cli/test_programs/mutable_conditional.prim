// Test mutable variable modified in conditional - exercises SSA phi nodes
import std.io.println

fn main() {
    let mut x = 1
    let cond = 5 > 3
    if cond {
        x = 10
    } else {
        x = 20
    }
    println(x)
}
