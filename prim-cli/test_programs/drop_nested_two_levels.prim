import std.io.println

struct Inner { id: i32 }
impl Drop for Inner {
    fn drop(edit self) { println(self.id) }
}
struct Mid { inner: Inner }
struct Outer { mid: Mid }

fn main() {
    let o = Outer { mid = Mid { inner = Inner { id = 9 } } }
    println(0i32)
}
