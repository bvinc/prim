import std.io.println

struct R { id: i32 }
impl Drop for R {
    fn drop(mut self) { println(self.id) }
}

// A `read` borrow does not drop the resource; only its owner does, once.
fn inspect(r: read R) -> i32 {
    return r.id
}

fn main() {
    let r = R { id = 6 }
    println(inspect(read r))
    println(0i32)
}
