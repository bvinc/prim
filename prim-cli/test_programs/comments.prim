// This is a line comment at the top
import std.io.println_i64

fn main() {
    let x = 42 // inline comment
    /* This is a block comment
       that spans multiple lines */
    let y = /* inline block */ 10
    println_i64(x + y) // prints 52
}
