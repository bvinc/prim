mod string

// Standard library string representation
// Note: This mirrors the current runtime array header approach.
// It intentionally uses a pointer and explicit length/capacity.
struct String {
    ptr: *const u8,
    len: usize,
    cap: usize,
}

